// const biggerStr = (str1, str2) =>(str1.length > str2.length ? str1 : str2)

// console.log(biggerStr("india","hisdustan"))

const biggerStr = ( str1, str2)=> (str1.length> str2.length ? str1: str2)

console.log(biggerStr("netherland","switzerland"))